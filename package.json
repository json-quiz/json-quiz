{
  "name": "quiz-format",
  "version": "0.0.1",
  "description": "Extensible quiz/question JSON format",
  "devDependencies": {
    "ajv": "^0.6.0",
    "mocha": "^2.2.5"
  },
  "scripts": {
    "test": "node_modules/.bin/mocha test --recursive"
  }
}
