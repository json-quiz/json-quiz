{
  "name": "quiz-format",
  "version": "0.0.1",
  "description": "Extensible quiz/question JSON format",
  "devDependencies": {
    "ajv": "^0.6.1",
    "fs-extra": "^0.21.0",
    "mocha": "^2.2.5",
    "shelljs": "^0.5.3"
  },
  "scripts": {
    "test": "node_modules/.bin/mocha --recursive",
    "clean": "node make clean",
    "doc": "node make doc",
    "preview": "node make preview",
    "publish": "node make publish"
  }
}
